import { ReactElement } from "react";
export declare function isMenuChildSelectable(child: ReactElement): boolean;
export declare const generateMenuItemId: (menuId: string, child: ReactElement, index: number) => string;
